{
  "name": "one-more-day-game",
  "version": "0.14.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "node --test tests/*.test.mjs",
    "audit:layout": "node scripts/check-layout.mjs",
    "audit:visual": "node --test tests/visual-contract.test.mjs",
    "audit:exploration": "node scripts/check-exploration.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^7.1.4"
  }
}
