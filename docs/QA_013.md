# 0.13 合并验收记录

日期：2026-09-08。测试环境：本项目隔离 `127.0.0.1:5174`，没有重置或读取 `localhost:5173` 的玩家存档。三路工作分工、标准和具体改动见 `VISUAL_ACCEPTANCE_013.md`、两份 `VISUAL_AUDIT_013_*` 与 `VISUAL_REVIEW_013.md`。

## 可重复检查

- `npm run build`：TypeScript 和生产构建通过。
- `npm run audit:layout`：八关 errors 均为空。
- `npm test`：66 项回归通过（逻辑/导航/危险、24 项视觉契约、拾取后碰撞释放、坠物残骸落点）。
- 改写两个旧断言：墙内收藏允许与封墙锚点重叠，门侧锁允许紧邻所属门；小物间距按尺寸早期预警，不再用家具统一 34px 圈强行推远。该测试仍不证明像素轮廓无重叠。
- 浅壁龛在已有轨道侧操作，因此未开墙时站位可达；改测收藏不可见、不可提前交互、破墙后出现、领取不产生主线口令。

## 实际画面与操作

- 两组作者逐关审看 8 张实际合成图；独立复核另看 DAY05—08、矿洞闭合及开墙未领取状态。主会话复看街景、办公室闭/开门、河湾、营地、雪站、瀑布开路、矿洞近景。不是只查看原始图片或坐标。
- 办公室：旧悬空书柜/平涂拉杆/木门槛不再出现，控制器绑定真实壁柱；消防门第一次生成位置错到护栏，已驳回，改用第二张同位置开态。主会话实际点击配电柜，角色沿地面抵达 `(1020,864)` 后才开谜题；正确解答后任务更新为检修拉柄。
- 办公室第二次近景驳回：原黑门实际朝电梯厅，旧拉柄站位 `(1002,289)` 压在墙顶。最终删除越墙地面折角、固定封锁该点，并在薄隔墙真实开门洞。主会话重走配电流程，点击壁柱面板后到 `(961,248)` 的茶水间地砖完成 `office-latch`，开态露出连续门槛；再实际鼠标穿门到电梯厅 `(1099.6,367.0)`。原黑门不再承担错误连接，打开门洞不吞掉地面点击。控制台 warning/error 均为空。
- 独立收尾复核又指出新门洞短裁片留下打印机上方旧墙纸下沿。延长同注册裁片至完整变化墙面，导航不变；主会话在实际开态合成图确认横切残片消失，避免把“穿门通过”当作拼片美术通过。
- 近景顺带发现的余波问题：吊顶旧五边形碎石成排停在墙上，改为小型散落碎片，只在真实地面保留残骸；预警仍使用落灰/声音，伤害时序不变。选择题随机排序不再叠加第二套 A/B/C 字母，实际配电题已验证显示序号 + 原支路名。
- 矿洞：主会话实际鼠标拾取小锤，角色到 `(476,252)` 后获得 `cave-hammer`；点击封墙到 `(401,284)` 后完成 `cave-wall`；手记显现在龛下半部，点击后角色到 `(394,286)`，获得 `cave-keepsake`。背包实际显示“矿工小锤”“矿工的夕阳手记”，主线仍 0/4，画面留下空龛而非旧热区。
- 大画面检查：看过矿洞扩大画面的主角、封龛、空龛与背景关系。办公室稳定场景点击全屏后画布扩大至 2576×1408，人物、门洞与 HUD 显示正常；内置浏览器中 DOM 读取的 Fullscreen API 状态与画面/按钮状态不一致，故只计实际大画面审查，不宣称已做完整跨浏览器全屏验收。
- 瀑布：原灰色连续道路被驳回；首张纹理过密的砖拼石台缩小后像沙砾带，再次驳回。最终采用单体天然石板的四变体，RGBA，沿原路线离散排列；相应废稿、最终稿、提示词和生产脚本均保留。

## 明确边界

这次修复画面与交互物件的身份、比例、位置和注册规则，没有宣称 8 关全部动作每帧都达到最终美术品质。完整背景树冠/山体分层、所有危险独立源头素材、更深空间谜题及真人无说明试玩仍是后续工作。原画绑定物的局部操作状态目前主要是贴片、暗屏、声音和结果反馈，并非每扇门都有逐帧转轴动画。

新增的苔石路径已去掉程序化灰路，但仍属于可继续打磨的过渡关卡资产；瀑布跨高差连接若做正式美术重绘，应该同时制作完整桥/台阶层，而不是继续靠静态贴片承担所有高度信息。
