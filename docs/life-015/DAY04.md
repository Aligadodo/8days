# DAY04 · v0.15 生活物资

数据 `src/campaign/life/day04.ts` 默认导出 LifePack。保留合格的 v0.14 记录器修复链、室内图和进出门数据；没有改旧 world、exploration 或共享引擎。逐张查看原瀑布图与水声观测所图后按原生 1672×941 坐标标定，站位用实际世界坐标。

## 数量与生命周期

原图 6 节点：0 容器、2 gather、4 inspect；室内 7 节点：2 container、2 gather、3 inspect。合计 13 个 LifeNode。所有 gather 再生为 300 秒在线游戏时间，不应离线/暂停刷新。DAY04 原图没有可以安全开启的真实箱桶，主任务已明确接受数量例外，不生成假补给箱。

已移除首稿重复旧湿苔/水轮/标本 inspect 的做法。新增独立石阶接缝、木栏柱、护岸石、桥端板和阅读木凳等实际部件。只有室内绳圈余料与迎客茶补给采用 `sharedDiscovery`，分别为 `d04-cord`、`d04-kettle`，其原始 baked/approach 完全复用；各共享发现只绑定一项生活内容。

## 实物覆盖与明确遗漏

| 场景/实物 | 配置与边界 |
|---|---|
| 下游阶旁植物簇 | gather `lower-fronds`：自然落叶纤维，`cloth` |
| 西岸吊桥北端岸边植物簇 | gather `bridge-fronds`：自然落叶纤维，`cloth` |
| 下游转角石缝、桥中木栏柱、下游护岸石、东岸桥端板 | 四个独立 inspect；不拆承重石木、不新增奖励 |
| 吊篮 | 原图确有容器外形，但悬在崖侧、承担原主线运输；不从远岸遥开或变成物资箱 |
| 水轮、阀门、石亭、崖壁灯窗、瀑布/睡莲/深水/树冠 | 原设施、主线或不可达背景；不把它们冒充可开容器/额外门 |
| 其余岸边植物、石头 | 未逐像素交互，尤其崖外、水中与高台边缘不创建虚假站位 |
| 检修台右缘真实敞口小黄铜罐 | container `parts-tin`：一份密封纤维余料，`cloth`；原来已敞口，不伪造旋盖动作 |
| 检修桌绳圈/短扳手 | gather `cord-offcuts`：只整理绳纤维，`cloth`；沿用原工具借用发现，扳手不消耗 |
| 左窗台花盆 | gather `window-cuttings`：允许领取已剪芳香草余枝，`herbs`，不摘活花 |
| 东墙公用水壶 | container `kettle-share`：揭盖倒一份迎客温茶，`tea`；盖回原位，原给后来者留温水的小事继续生效 |
| 猫水碗 | inspect `cat-bowl`，看水面、不拿猫饮水或碗 |
| 阅读桌前木凳 | inspect `reading-stool`，看榫脚，不拆木料 |
| 隔墙西侧花盆 | inspect `partition-pot`，不拿走盆栽 |
| 大压力表、记录器与纸卷、三枚标本、旅行簿、画卡 | 保留原 v0.14 交互；不重复 inspect、不把压力表或机器当宝箱 |

室内除小罐和公用壶，没有漏称为“私人所以不开”的普通箱柜；猫碗/花盆不作为随机物资容器。温茶只出茶、纤维只出纤维，不使用容易掉水果/矿石的粗池。

## 验证

```powershell
# workdir D:/projects/OneMoreDayGame
node design/life-015/day04/audit.mjs
```

`design/life-015/day04/audit-results.json` 保存逐点初始/完整 flag、冲突与站位。13/13 在完整 flag 下精确可达，每条导航线段 visible，7 个室内节点均可达返回口。仅 `east-bridge` 初始不可达，需要原 `lily-path` 连通东岸；其余原图五点初始可达。室内按房间进入后初始状态验证；真实入室仍遵守原上层门 `sharedTarget=exit` 与 `lily-path`。

共享发现/轮廓/站位完全一致；所有 ID 唯一，限定池有效、300 秒再生；无与主线及既有发现重叠而未共享的轮廓。未修改任何导航文件，未调用 nearest 猜站位。浏览器真实开合、拾取、背包、再生/幂等与加载持久化交主任务统一验收，未操作用户游戏与存档。

## 冻结交付与开态边界

按主任务要求冻结 **13 节点（外6、内7）**。最新独立审计0 failure：完整flag **13/13** 可达，原图初始5/6可达（东岸桥端需 lily-path），室内7/7可达且能返回；限定池、共享轮廓/站位、相交热区检查通过。`npx tsc --noEmit` 通过，中央 `life-content.test.mjs` 筛选 DAY02/04 的8项测试通过。

内置 imagegen 尝试为小罐生成开态，完整提示在 `design/life-015/day04/prompts.md`，候选源图 `parts-tin-open-source.png`。独立浏览器六倍放大原图/候选后发现：原物本来就是敞口，候选没有产生可信且必要的新开盖变化，因此**拒绝接入此候选**；仅在design保留审计源稿，不在生产目录留下未使用素材。小罐数据按“查看罐内、取一份纤维、空罐原位保留”表述。迎客茶为揭盖倒取后复位，尚无单独盖子动画。没有将压力表、记录器或小罐重画成宝箱。

主任务报告了独立于静态导航的共享动态问题：`window-cuttings` 行走在 `(291.0097844,527.5048922)` 前往 `(238,492)` 时卡顿，已交空间与runtime代理协作。作者没有为绕过运行时问题搬动真实站位；**静态可达不代表动态移动已通过**，最终游戏移动/交互、再生计时/存档由主任务验收。
